mkdir LocalRepo
cd LocalRepo

git init
git branch -m main
git remote add origin <-link->

git add .
git commit -m "message"
git push origin main

git checkout -b new_branch
git checkout new_branch

undoing changes
* Staged changes
git reset <-filename->
git reset
* Commited changes
git reset HEAD~1 (one commit)
